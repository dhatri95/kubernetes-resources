kind: Service
apiVersion: v1
metadata:
  name: expense-cip
  labels:
    example: clusterIP
spec:
  selector:
    environment: dev
    project: expense
    component: frontend
    example: testing-services  
  ports:
  - port: 80 #clusterIP port
    targetPort: 80 #pod port
